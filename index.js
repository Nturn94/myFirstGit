

var AddNums = function(x, y){
    (newValue = x+y);
    return newValue;

}

// Comment, comment, comment.

newValue = 0;

AddNums(2000, 3000);

console.log(newValue);

AddNums(2, 7);
console.log(newValue);

// code that adds numbers
